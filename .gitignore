# OS / editor
.DS_Store
*~
*.swp

# Elmer outputs (large binaries — regenerated on each run)
*.result
*.result.*
*.vtu
*.pvtu
*.pvd
ELMERSOLVER_STARTINFO

# Job logs (regenerated)
*.out
*.err
slurm-*.out

# Cost / m1qn3 diagnostic files
Cost_*.dat
CostReg_*.dat
GradientNorm_*.dat
M1QN3_*.out

# GIS source datasets (download from BedMachine / MEaSUREs / Pattyn)
*.tif
*.nc
*.gpkg
*.qgz

# Mesh partition directories (regenerated by ElmerGrid)
partitioning.*/
mesh2D_*_refined/

# Boundary CSV raw exports — keep only the cleaned version
Antarctica_Boundary.csv
Antarctica_Boundary_xy.csv

# Notion / draft binary docs
*.pptx
*.pptx.bak.*

# Local Claude / IDE files
.claude/
.vscode/
